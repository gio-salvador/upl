# sc-open-pr manifest. The skill stays generic; project specifics live here.
default_branch: main
branch_prefixes: [feat, fix, chore, docs, refactor, test, style, perf, build, ci, revert]

pre_pr_checks:
  - bash scripts/check.sh

pr_template: .github/pull_request_template.md
