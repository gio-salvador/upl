{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Environment declaration",
  "description": "Every environment variable this repository reads, declared before use (Goal G22). Secrets never appear here: they go through sc-secret.",
  "type": "object",
  "required": ["env"],
  "properties": {
    "version": { "type": "integer" },
    "env": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "purpose"],
        "properties": {
          "name": { "type": "string", "pattern": "^[A-Z][A-Z0-9_]*$" },
          "required": { "type": "boolean" },
          "default": { "type": ["string", "integer", "boolean"] },
          "purpose": { "type": "string", "description": "One line. A variable nobody can explain is a variable nobody can remove." }
        }
      }
    }
  }
}
